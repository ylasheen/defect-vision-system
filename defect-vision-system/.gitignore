__pycache__/
*.pyc
.venv/
venv/
.env
.DS_Store
.ipynb_checkpoints/
.pytest_cache/
.streamlit/
mlflow_runs/
models/logs/*.log

# Generated synthetic dataset (large, many small files, regenerated by
# make_dataset.py + prepare_splits.py in seconds). NOTE: models/saved_models/
# and models/artifacts/ are intentionally NOT excluded -- they're small
# (~1-2 MB) and the API/dashboard need them committed to work out of the box
# without retraining first.
data/raw/
data/processed/
